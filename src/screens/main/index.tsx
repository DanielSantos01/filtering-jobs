import React from 'react';

import { Header } from '../../components';
import { Section } from './styles';

const Main: React.FC = () => {
  return (
    <Section>
      <Header />
    </Section>
  );
};

export default Main;
